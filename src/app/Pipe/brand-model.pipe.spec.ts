import { BrandModelPipe } from './brand-model.pipe';

describe('BrandModelPipe', () => {
  it('create an instance', () => {
    const pipe = new BrandModelPipe();
    expect(pipe).toBeTruthy();
  });
});
