import { RentalRecordPipe } from './rental-record.pipe';

describe('RentalRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
