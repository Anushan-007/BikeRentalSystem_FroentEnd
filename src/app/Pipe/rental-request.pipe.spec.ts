import { RentalRequestPipe } from './rental-request.pipe';

describe('RentalRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
